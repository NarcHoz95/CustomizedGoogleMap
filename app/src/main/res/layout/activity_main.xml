<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/mainFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.v7.widget.Toolbar
        android:id="@+id/mainToolbar"
        android:layout_margin="8dp"
        android:theme="@style/ThemeOverlay.AppCompat.Light"
        android:background="@color/colorWhiteTrans"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/searchEditText"
            android:inputType="text"
            android:hint="@string/search_hint"
            android:textColorHint="@android:color/darker_gray"
            android:visibility="invisible"
            android:imeOptions="actionSearch"
            android:background="@color/colorWhiteTrans"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </android.support.v7.widget.Toolbar>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/mainActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:src="@drawable/ic_settings_black_24dp"
        app:fabSize="normal"
        app:backgroundTint="@android:color/background_light"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/googleMapAction"
        android:visibility="gone"
        app:fabCustomSize="30dp"
        android:layout_margin="20dp"
        app:layout_constraintTop_toTopOf="@id/mainActionButton"
        app:layout_constraintBottom_toBottomOf="@id/mainActionButton"
        app:layout_constraintEnd_toStartOf="@id/mainActionButton"
        android:src="@drawable/ic_icons8_google"
        app:backgroundTint="@android:color/white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/listActionButton"
        app:fabCustomSize="30dp"
        android:visibility="gone"
        app:backgroundTint="@android:color/white"
        android:layout_margin="20dp"
        app:layout_constraintBottom_toTopOf="@id/mainActionButton"
        app:layout_constraintStart_toStartOf="@id/mainActionButton"
        app:layout_constraintEnd_toEndOf="@id/mainActionButton"
        android:src="@drawable/ic_dashboard_black_24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</android.support.constraint.ConstraintLayout>